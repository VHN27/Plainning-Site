# Programmation Web

## Installation

- Dans le root, exécuter : `npm install`
- Gérer la db et le fichier .env (les infos seront ajoutées plus tard)

## Utilisation

- Dans le root, exécuter : `node server.js`
- Le site est alors disponible à l'adresse : http://localhost:5000
